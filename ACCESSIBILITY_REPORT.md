# Accessibility report

## Changes in this refinement

The shared brand link accessible name was normalized from `STR Sgomberi — Home` to `STR Sgomberi` across all 50 HTML documents so the accessible name matches the visible brand text.

## Existing patterns

The snapshot includes a skip link, semantic header/nav/main/footer landmarks, visible focus styles, button state attributes for the mobile menu, alt text on content images, and reduced-motion handling.

## Remaining verification

Run keyboard navigation, screen-reader landmark checks, color-contrast checks, and responsive audits on the deployed origin. No automated accessibility evidence was included in the cloned branch.
